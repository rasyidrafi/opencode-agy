/**
 * OpenCode loads the built plugin through this stable package entrypoint.
 * Build with `npm run build` before registering this package or file URL.
 */
export { AntigravityCliPlugin, default } from "./dist/index.js";
